import { AvatarCatalog } from "../Login/AvatarCatalog";
import { LoginPage } from "../Login/LoginPage";
import { ProfileButton } from "../Login/ProfileButton";

it('Avatars catalog test', () => {
    expect(AvatarCatalog)
})

it('Login page test', () => {
    expect(LoginPage)
})

it('Profile buttons test', () => {
    expect(ProfileButton)
})