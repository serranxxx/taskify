import { HeaderApp, HeaderQuote, ProjectCard, Projects, TaskCard, Tasks } from "../Layout"


it('Task cards test', () => {
    expect(TaskCard)
})

it('Tasks test', () => {
    expect(Tasks)
})

it('Projects cards test', () => {
    expect(ProjectCard)
})

it('Projects test', () => {
    expect(Projects)
})

it('Header app test', () => {
    expect(HeaderApp)
})

// it('Header quote test', () => {
//     expect(HeaderQuote)
// })